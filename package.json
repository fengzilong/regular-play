{
  "private": true,
  "scripts": {
    "boot": "lerna bootstrap",
    "dist": "lerna run dist",
    "deploy": "lerna run deploy",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.38"
  }
}
